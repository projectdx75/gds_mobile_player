{
  "name": "flashplex",
  "private": true,
  "version": "0.2.4",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "version:bump": "bash scripts/version-bump.sh",
    "android:build": "npm run tauri android build -- --target aarch64",
    "android:shield": "npm run tauri android build -- --target aarch64"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/plugin-http": "^2.5.6"
  }
}
